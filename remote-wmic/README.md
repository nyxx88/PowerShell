A simple PowerShell script showing how to deliver and execute a payload from one Windows host to another Windows host. Assumption is that the local administrator password of the remote host is known.

In this example, the sample payload is a script that is used to download a Falcon sensor installer and then install it remotely.
